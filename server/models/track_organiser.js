
class TrackOrganiser{
  // constructor(array){
  //   this.array = array;
  //   this.albumArray = [];
  //   this.uniqueAlbumArray = [];
  // }

// allAlbums = router.get 'ALL ALBUMS'
// allTracks = router.get 'ALL TRACKS'
//
// allTracks.forEach(track,=>{
// push track onto a 'track' array within 'allAlbums'
// })





//
//
//   getAlbums(){
//     array=this.array;
//     albumArray=array.map(object,=>{
//       const album = {};
//       album.id = object.album_id;
//       album.title = object.album_title;
//       album.artist = object.artist;
//       album.year = object.year;
//       album.genre = object.genre;
//       album.label = object.label;
//       return album;
//     });
//
//   };
//
// isUnique(albums) {
//     if(this.album.indexOf(albumArray) != this.album.lastIndexOf(albumArray);
//   }
//
}
module.exports = TrackOrganiser;
